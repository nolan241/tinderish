<tr>
	<!-- image -->
	<th scope="row"><%= image_tag "#{match.avatar(:thumb)}", size: "30x30" %></th>
	
	<!-- name -->
	<td><%= link_to match.name, profile_user_path(match.id) %></td> 
	
	<!-- matched date -->
	<td><%= match.updated_at.strftime('%m/%d/%Y') %></td>
	
	<!-- buttons --> 
	<td class="text-right">
		
		<%= link_to matches_get_email_path(id: "#{match.id}"), :data => {toggle: "tooltip", placement: "top"}, title: "Get Email", remote: :true do %>
			<i class="fa fa fa-envelope"></i>
		<% end %>
		
		<a href="" data-toggle="tooltip" data-placement="top" title="Get Email"><i class="fa fa fa-envelope"></i></a>
		<%= link_to destroy_friendships_path(friend_id: match.id), :data => {toggle: "tooltip", placement: "top", confirm: "Are you sure you want to unmatch with #{match.name}?" }, title: "Unmatch", id: "delete_friendship", method: :delete  do %>
			<i class="fa fa-times"></i>
		<% end %>
	</td>
</tr>

this is not showing up as partial